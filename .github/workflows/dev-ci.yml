name: Dev CI Pipeline

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop

jobs:
  lint-and-test:
    uses: ./.github/workflows/ci-base.yml
    with:
      run_integration: false
      environment: 'dev'
