name: Prod CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test-build-publish:
    uses: ./.github/workflows/ci-base.yml
    with:
      run_integration: true
      environment: 'prod'
